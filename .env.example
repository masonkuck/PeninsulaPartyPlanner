# Copy this file to .env.local for local development.
# .env.local is gitignored so your key never gets committed.
# For production builds via GitHub Actions, set VITE_ORS_KEY as a repo secret instead.

VITE_ORS_KEY=
